import { useState } from "react";
import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";
import { Box,Drawer,IconButton,MenuItem,TextField,Typography } from "@mui/material";
import AddIcon from "@mui/icons-material/Add"; import EditIcon from "@mui/icons-material/EditOutlined"; import DeleteIcon from "@mui/icons-material/DeleteOutlineOutlined"; import CloseIcon from "@mui/icons-material/Close";
import { Link } from "react-router-dom";
import { createCategory,deleteCategory,getCategories,updateCategory,type Category,type CategoryInput } from "../../api/catalogApi";
import Button from "../../components/common/Button/Button";
import "../products/ProductsPage.css";
import "./CategoriesPage.css";

const empty:CategoryInput={name:"",description:"",status:"ACTIVE"};
const CategoriesPage=()=>{const qc=useQueryClient();const [search,setSearch]=useState("");const [open,setOpen]=useState(false);const [editing,setEditing]=useState<Category|null>(null);const [form,setForm]=useState<CategoryInput>(empty);const [error,setError]=useState("");const query=useQuery({queryKey:["categories",search],queryFn:()=>getCategories(search)});const save=useMutation({mutationFn:()=>editing?updateCategory(editing.id,form):createCategory(form),onSuccess:()=>{qc.invalidateQueries({queryKey:["categories"]});qc.invalidateQueries({queryKey:["products"]});setOpen(false)},onError:(e:any)=>setError(e.response?.data?.detail||e.message)});const remove=useMutation({mutationFn:deleteCategory,onSuccess:()=>qc.invalidateQueries({queryKey:["categories"]}),onError:(e:any)=>alert(e.response?.data?.detail||e.message)});const show=(c?:Category)=>{setEditing(c||null);setForm(c?{name:c.name,description:c.description||"",status:c.status}:empty);setError("");setOpen(true)};return <Box className="catalog-page"><Box className="catalog-summary"><Box className="catalog-stat"><span className="catalog-stat__icon purple">▦</span><div><small>Total Categories</small><strong>{query.data?.total||0}</strong></div></Box><Box className="catalog-stat"><span className="catalog-stat__icon green">✓</span><div><small>Active Categories</small><strong>{query.data?.items.filter(x=>x.status==="ACTIVE").length||0}</strong></div></Box></Box><Box className="catalog-tabs"><Link to="/products">Products</Link><Link className="active" to="/categories">Categories</Link></Box><Box className="catalog-panel"><Box className="category-top"><TextField size="small" placeholder="Search categories by name..." value={search} onChange={e=>setSearch(e.target.value)}/><Button startIcon={<AddIcon/>} onClick={()=>show()}>Add Category</Button></Box><Box className="catalog-table"><table><thead><tr><th>Category Name</th><th>Description</th><th>Status</th><th>Products</th><th>Actions</th></tr></thead><tbody>{query.data?.items.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td className="category-description">{c.description||"—"}</td><td><span className={`catalog-status ${c.status.toLowerCase()}`}>{c.status==="ACTIVE"?"Active":"Inactive"}</span></td><td>{c.productCount}</td><td><Box className="category-actions"><IconButton onClick={()=>show(c)}><EditIcon/></IconButton><IconButton color="error" onClick={()=>confirm("Delete this category?")&&remove.mutate(c.id)}><DeleteIcon/></IconButton></Box></td></tr>)}</tbody></table>{!query.data?.items.length&&<p className="catalog-empty">No categories found.</p>}</Box></Box><Drawer anchor="right" open={open} onClose={()=>setOpen(false)}><Box className="catalog-drawer"><Box className="catalog-drawer__head"><Typography component="h2">{editing?"Edit Category":"Add Category"}</Typography><IconButton onClick={()=>setOpen(false)}><CloseIcon/></IconButton></Box>{error&&<p className="catalog-error">{error}</p>}<TextField required label="Category Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><TextField multiline rows={4} label="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><TextField select label="Status" value={form.status} onChange={e=>setForm({...form,status:e.target.value as CategoryInput["status"]})}><MenuItem value="ACTIVE">Active</MenuItem><MenuItem value="INACTIVE">Inactive</MenuItem></TextField><Box className="catalog-drawer__actions"><Button variant="outlined" onClick={()=>setOpen(false)}>Cancel</Button><Button loading={save.isPending} onClick={()=>save.mutate()}>Save Category</Button></Box></Box></Drawer></Box>};export default CategoriesPage;
